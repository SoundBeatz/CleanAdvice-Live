services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped

  agent:
    build: ./agent
    env_file:
      - .env.local
    restart: unless-stopped
    depends_on:
      - web
